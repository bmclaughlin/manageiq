%div{:id => "#{name}_div", :style => "width:100%;height:100%;"}
  %div{:id => "#{name}box", :style => "width:100%;height:100%;color:#000; overflow: hidden"}
    - tree_partial = %w(cb_reports_tree).include?(name) ? 'layouts/bootstrap_treeview' : 'layouts/dynatree'
    = render  :partial  => tree_partial,
              :locals   => tree.locals_for_render
